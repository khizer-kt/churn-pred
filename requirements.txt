# Core data / modelling
pandas>=2.2,<3
numpy>=2.0,<3
scikit-learn>=1.9,<2   # must match the version that pickled artifacts/pipeline.joblib
joblib>=1.4
pyarrow>=16

# App
streamlit>=1.40
groq>=0.13
python-dotenv>=1.0

# Notebook / eval / dev
matplotlib>=3.9
pyyaml>=6.0
pytest>=8.0
